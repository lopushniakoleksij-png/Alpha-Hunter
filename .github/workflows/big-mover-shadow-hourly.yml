name: Big-Mover Shadow Hourly

on:
  push:
    branches:
      - "feature/big-mover-signature-engine-20260913"
    paths:
      - ".github/workflows/big-mover-shadow-hourly.yml"

permissions:
  contents: read

jobs:
  runner-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm hosted runner starts
        run: echo BIG_MOVER_SHADOW_RUNNER_OK
