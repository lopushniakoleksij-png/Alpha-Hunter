# ALPHA HUNTER — CURRENT PRODUCTION STATE

Last updated: 2026-08-30 22:40 UK
Repository: lopushniakoleksij-png/Alpha-Hunter
Branch: fix/p0-bitget-fill-traceability-20260830

## PURPOSE

This file is the mandatory Alpha Hunter production handoff record.

Before any new ChatGPT conversation changes Alpha Hunter production, it must read this record and establish the current state.

No production work may rely only on chat history.

## WORKFLOW RULE

PLAN → EXECUTE → TEST → VERIFY → RECORD → COMMIT → NEXT

No meaningful production step is complete until it is recorded.

## 2026-08-30 P0 TRACEABILITY STABILIZATION UPDATE

This update supersedes the older point-in-time repository and working-tree
observations below. Historical research notes remain evidence and are not
production permission.

Classification: P0 STABILIZATION / READ-ONLY EXCHANGE INTEGRATION

Protected primary hourly task: REMAINS ENABLED AND UNCHANGED

Production baseline:

- `origin/main`: `89e42a9`
- Open diagnostic PR #3 remains separate and unmerged
- Repair branch: `fix/p0-bitget-fill-traceability-20260830`
- Current repair branch head: `2b7c1e2`

Executed:

- Replaced the order-specific `/api/v2/mix/order/fill-history` usage with the
  documented account-wide futures fill endpoint `/api/v2/mix/order/fills` for
  rolling account fill reconciliation
- Added strict `fillList` / `endId` schema validation and cursor pagination
- Added window, endpoint, page, count, timestamp, completeness, and schema
  coverage telemetry
- Changed zero-fill, malformed, unconfigured, stalled, and truncated coverage
  from false `PASS` to `INCOMPLETE`
- Changed unlinked open-like fills to a non-zero traceability failure exit
- Added `--fill-history-smoke-test`, which performs no orders, Supabase writes,
  cache writes, snapshot writes, or account changes

Verification:

- Python compilation: PASS
- Focused traceability tests: 12 PASS
- Full regression suite: 30 PASS
- Diff validation: PASS
- No-credential smoke-test safety behavior: PASS; reported `INCOMPLETE` and
  exited 2 without writes
- Credentialed live read-only smoke test: PENDING before merge

Not changed:

- Trading thresholds, gates, scoring, R:R, candidate selection, or permissions
- Bitget order placement, modification, cancellation, leverage, or position mode
- Scheduler, deployment, protected automation, or PR #3

## CURRENT CORE STABILIZATION DIRECTIVE

Until Stage 0 exits successfully, this is the governing production plan:

1. DO NOT expand architecture, tune trading thresholds, add features, or perform cleanup while a genuine P0 blocker remains.
2. KEEP Alpha Hunter Primary Hourly enabled, hourly, and without COUNT / UNTIL / end date.
3. FIRST validate the Bitget fill-reconciliation repair using the credentialed read-only smoke test on the production Mac.
4. MERGE the P0 repair only after evidence shows `status=CONNECTED`, `complete=true`, `schema_validated=true`, `fill_count > 0`, credible timestamps/window, and no write/order path.
5. THEN reconcile `hourly.py` and `hourly_production.py` into one canonical production orchestration path without changing trading rules.
6. PUT the canonical production path on a verified hourly scheduler.
7. REQUIRE three consecutive genuine live production cycles with fresh data, snapshot persistence, Supabase/state persistence, readiness diagnostics, trustworthy traceability, and no unexplained READY/execution gaps before declaring Stage 0 complete.
8. PR #3 readiness-root-cause diagnostics stays separate until the P0 traceability repair is validated and integrated safely.

Stage 0 exit condition: one reliable live hourly Alpha Hunter production loop, no unresolved P0 blocker, production code aligned with GitHub main, and end-to-end production evidence trusted.

Next production step:

1. Run the credentialed read-only fill-history smoke test on the production Mac.
2. Review the returned fill count, page coverage, schema status, time window, and unlinked opening fills.
3. Merge only after the live evidence is complete and trusted.

## HISTORICAL STATE BELOW

The remainder of this file predates the current P0 stabilization update. It is retained as historical evidence only and must not override the current directive above.

## CURRENT PRODUCTION / RESEARCH STATE — HISTORICAL

Current active research: V7.10 Early Execution / RR Shadow Research

Classification: SHADOW / RESEARCH
Production permission: NONE

V7.10 must remain read-only until explicitly promoted after testing and verification.

Observed V7.10 diagnostic cases:

WLDUSDT
Class: WRONG_DIRECTION_BAD_RR
Direction: SHORT
Executable RR: 4.78
Captured RR: 0.01
Lost RR: 4.78
Move: -0.03

CAPUSDT
Class: WRONG_DIRECTION_BAD_RR
Direction: LONG
Executable RR: 0.86
Captured RR: 0.13
Lost RR: 0.73
Move: 0.38

V7.10 run explicitly reported:

- READ-ONLY SHADOW RESEARCH
- No Supabase rows written
- No trade permission generated

## LOCAL WORKING TREE LAST OBSERVED — HISTORICAL

Modified:
production_runner.py

Untracked:
data/
restore_previous_snapshot.py
tests/test_restore_previous_snapshot.py

This local state has NOT yet been reconciled with GitHub.

Do not overwrite, discard, commit, or promote these changes until their purpose and diff are verified.

## PERMANENT HANDOFF RULE

Every production session must record:

1. What we planned
2. What we executed
3. Files changed
4. Tests performed
5. Test results
6. Failures discovered
7. Decisions made
8. Production/research classification
9. Git commit
10. Exact next step
